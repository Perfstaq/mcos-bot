{
  "name": "@mcos/render",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Content Studio render engine — the Remotion bundle root. Compositions are typed React components taking a RenderPlan as props; nothing in here computes its own timing (03_RENDER_PIPELINE §4). Per ADR-5, this is the ONLY package allowed to import `remotion` — see the containment guard at apps/api/tests/render-containment.test.ts.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js",
    "./plan": "./dist/plan.js",
    "./gates/types": "./dist/gates/types.js",
    "./gates/g1a": "./dist/gates/g1a.js",
    "./compositions/Reel": "./dist/compositions/Reel.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "remotion": "^4.0.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@remotion/cli": "^4.0.0",
    "@types/react": "^18.3.17",
    "@types/react-dom": "^18.3.5",
    "typescript": "^5.7.2"
  }
}
