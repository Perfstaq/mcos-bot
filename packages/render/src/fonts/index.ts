/**
 * fonts/ — scaffold placeholder.
 *
 * Owned by Agent M/T: port embedded data-URL fonts (`fonts.ts` +
 * `fontdata.generated.ts` + `scripts/embed-fonts.mjs` from founder-journey,
 * ARCHITECTURE.md §1.1) with 02_MOTION_SYSTEM §7's typography tokens
 * (Anton/Playfair/Inter-class, OFL) swapped in. Embedded data-URL fonts are
 * preferred over `@remotion/google-fonts` — no `delayRender` hang risk,
 * offline-deterministic on Lambda (ARCHITECTURE.md §10).
 */
export {};
